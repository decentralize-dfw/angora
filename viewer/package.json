{
  "name": "angora-21-viewer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/prepare-native.mjs",
    "dev": "vite --host 0.0.0.0 --port 4173",
    "prebuild": "node scripts/prepare-native.mjs",
    "build": "vite build",
    "build:pages": "vite build --mode pages && node scripts/stage-pages.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "7.1.3"
  }
}
